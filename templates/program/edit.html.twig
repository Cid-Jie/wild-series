{% extends 'base.html.twig' %}

{% block title %}Edit Program{% endblock %}

{% block body %}
	<main class="container_all_series">
		<div class="container">
			<div class="container_series">
				<h1 class="text-center mt-5 mb-5">Modifier une série</h1>
				{{ include('program/_form.html.twig', {'button_label': 'Update'}) }}
				<p class="text-center">
					<a href="{{ path('program_list') }}">back to list</a>
				</p>
				<p class="text-center">
					{{ include('program/_delete_form.html.twig') }}
				</p>
			</div>
		</div>
	</main>
{% endblock %}