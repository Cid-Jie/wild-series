{% extends 'base.html.twig' %}

{% block title %}Edit Category{% endblock %}

{% block body %}
	<main class="container_all_series">
		<div class="container">
			<div class="container_series">
				<h1 class="text-center mt-5 mb-5">Modifier une catégorie</h1>

    {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('category/_delete.html.twig') }}
<p class="text-center">
    <a href="{{ path('app_index') }}">back to list</a>
</p>

{% endblock %}
