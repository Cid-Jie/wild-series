{# templates/category/new.html.twig #}

{% extends "base.html.twig" %}

{% block body %}
	<main class="container_all_series">
		<div class="container">
			<div class="container_series">
				<div class="text-center mt-5 mb-5">
					<h1 class="text-center mt-5 mb-5">Ajouter une catégorie</h1>
                    <div>
					{{ form_start(form) }}

                        <div class="my-custom-class-for-errors">
                            {{ form_errors(form) }}

                        <p class="text-center mt-5 mb-1">
                            {{ form_label(form.name) }}
                        </p>
                        <p class="text-center mt-1 mb-5">
                            {{ form_widget(form.name) }}
                        </p>
                    </div>
					<button class="btn btn-secondary btn-lg mt-5 mb-5" type="submit">Ajouter</button>

					{{ form_end(form) }}

				</div>
			</div>
		</div>
	</main>
{% endblock %}
