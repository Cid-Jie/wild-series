{% extends 'base.html.twig' %}

{% block title %}Edit Actor
{% endblock %}

{% block body %}
	<main class="container_all_series">
		<div class="container">
			<div class="container_series">
				<h1 class="text-center mt-5 mb-5">Modifier un acteur</h1>

				{{ include('actor/_form.html.twig', {'button_label': 'Update'}) }}
				<p class="text-center">
					<a href="{{ path('app_actor_index') }}">back to list</a>
				</p>
				<p class="text-center">
					{{ include('actor/_delete_form.html.twig') }}
				</p>
			</div>
		</div>
	</main>
{% endblock %}
